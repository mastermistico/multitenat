<%= form_with(model: ac_tenant, local: true) do |form| %>
  <% if ac_tenant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ac_tenant.errors.count, "error") %> prohibited this ac_tenant from being saved:</h2>

      <ul>
        <% ac_tenant.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
